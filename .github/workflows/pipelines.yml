name: Pipelines
on:
  push:
    branches:
      - action-docker-ci
jobs:
  deployment:
    runs-on: ubuntu-latest
    steps:
      - name: Deployment
        uses: digitalocean/app_action@main
        with:
          app_name: gift-app
          token: ${{ secrets.DIGITAL_OCEAN_TOKEN }}